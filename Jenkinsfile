pipeline {
    agent any

    stages {
        stage('Init') {
            steps {
                echo 'Init Jenkinsfile ...'
            }
        }
        stage('Echo') {
            steps {
                echo 'Running ECHO ...'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing ...'
            }
        }
    }
}